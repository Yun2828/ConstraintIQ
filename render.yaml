services:
  - type: web
    name: constraint-iq-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGINS
        value: "https://constraint-iq.vercel.app"
      - key: PORT
        value: "8000"
